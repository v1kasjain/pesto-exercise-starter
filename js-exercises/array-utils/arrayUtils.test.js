//TODO: Write your own test cases.
import { forEach,
  map,
  filter,
  reduce, } from './arrayUtils';

describe('array utils', () => {
  it('should return true', () => {
    expect(true).toBe(true);
  });
});