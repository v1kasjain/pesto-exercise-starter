
// Don't change the export values.
// You can use the function in the Math module

const sqrt = numberToBeSquareRoot => Math.sqrt(numberToBeSquareRoot);
const power = (numberToBePower, nTimes) => Math.pow(numberToBePower, nTimes);
const round = numberToBeRound => Math.round(numberToBeRound);

export {
  sqrt,
  power,
  round,
};
